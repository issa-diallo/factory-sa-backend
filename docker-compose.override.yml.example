version: '3.8'
services:
  backend:
    environment:
      - NODE_ENV=development
      - NODE_PATH=/app/src
    volumes:
      - ./:/app
      - /app/node_modules
    command: pnpm dev
    ports:
      - "5555:5555"